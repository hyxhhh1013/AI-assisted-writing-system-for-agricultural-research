# **农业碳基肥与盐碱地改良科研 AI 辅助系统**

## **项目规划与业务需求说明书 (V1.0)**

### **一、 项目背景与立项动因 (Why)**

**1\. 业务痛点与现有工具的局限性**

目前，实验室聚焦于“水稻/烟草的碳基肥改良”及“盐碱地种植（海水稻）”等高度垂直的农业生态研究。前期，团队曾斥资（约 3000 元）采购了外部第三方“解螺旋”论文辅助系统。但在实际使用中，生成质量极差，存在生搬硬套、术语错误等严重问题。

其根本原因在于：外部系统是基于**生物医学、临床病历**等数据训练的“医学专属智能体”。让医学 AI 处理农田生态与碳基肥数据，属于底层知识库的严重错配。团队的核心需求，无法通过购买市面上的闭源通用/医学辅助平台来解决。

**2\. 立项契机**

随着大语言模型（LLM）与检索增强生成（RAG）技术的成熟，课题组完全具备了利用自有实验数据和过往优秀文献，搭建\*\*“实验室专属农业 AI 论文助手”\*\*的条件。通过低成本的技术重构，可以极大缩短团队成员从“得出实验数据”到“完成论文初稿”的周期。

### **二、 项目核心目标 (What)**

本项目旨在搭建一套供实验室内部使用的 **Web 端的 AI 辅助写作平台**。

该系统将摒弃“一键生成全篇”的不切实际幻想，参考成熟平台的“提示词链（Prompt Chaining）”模式，将论文写作拆解为标准的流水线（SOP）。系统将深度绑定实验室过往累积的数百篇高质量垂直文献（知识库），在生成前言、实验方法、数据结论时，强制 AI 模仿本实验室的行文风格和专业术语。

### **三、 核心需求分析 (Requirements)**

#### **1\. 业务功能需求**

* **专属领域知识库 (RAG)**：支持将团队整理分类的文献（按水稻、烟草、碳基肥、盐碱地分类的 PDF/Word）构建为私有向量数据库，确保 AI 生成内容“言之有物，有迹可循”。  
* **结构化分步写作 (Step-by-Step)**：  
  * 提取与分析：输入 Excel/CSV 实验数据，AI 提取核心指标。  
  * 提纲生成：输入研究方向，生成标准的论文大纲。  
  * 模块化生成：分步骤生成摘要、前言（大背景-小背景-引出研究）、材料与方法、结果与讨论。  
* **数据安全与隔离**：确保实验一手数据不泄露给公共开源池。

#### **2\. 系统运维与交互需求**

* **多端协同访问**：系统需采取 B/S 架构（网页端），团队成员无需安装任何软件，在各自的电脑上通过浏览器（输入内网 IP 或特定域名）即可并发使用，解决共用一台物理机的排队痛点。  
* **极简交互**：UI 界面要求极简，以表单填写、文件上传和文本框交互为主，降低非计算机专业人员的学习门槛。

### **四、 技术选型与架构规范 (How)**

为兼顾开发效率、极低的硬件成本与代码的可维护性，本项目将严格按照以下现代前端+低代码 AI 后台的架构进行部署：

**1\. 技术栈拆解**

* **前端展示层**：Next.js (App Router) \+ Tailwind CSS \+ TypeScript。负责用户交互、表单状态管理与结果展示。  
* **核心 AI 业务层**：使用开源的 Dify 或 FastGPT 平台作为后端编排大脑。负责处理文献切片、向量化存储、RAG 检索以及提示词链的组合。  
* **大语言模型层 (LLM)**：通过 API 接入云端头部大模型（首选 DeepSeek-V3 或 阿里 Qwen-Max），负责最终的逻辑推理和文本生成。

**2\. 核心架构规范 (针对 AI 辅助编程 Vibecoding)**

为避免 AI 自动生成代码导致的“屎山效应”，项目确立以下底层架构原则，并写入全局 .cursorrules：

* **彻底解耦**：前端组件（.tsx）仅负责渲染，所有大模型调用、知识库检索必须封装在独立的 services 层。  
* **组件原子化**：严禁编写超过 300 行的超大组件文件。  
* **严格类型约束**：所有前后端数据流转必须经过 TypeScript Interface 校验。

### **五、 项目推进时间线 (Timeline)**

项目预计总开发周期为 **4 至 6 周**，采用敏捷开发模式，分三个阶段交付：

#### **第一阶段：最小可行性验证 MVP (第 1 \- 2 周)**

* **目标**：不涉及复杂 UI，跑通核心数据流。证明“我们的数据 \+ Dify 知识库 \+ 大模型”能写出像样的专业段落。  
* **任务**：  
  1. 收集首批核心文献（100篇左右），在 Dify 平台完成向量化知识库搭建。  
  2. 编写测试版提示词，跑通“输入粗糙实验结论 \-\> 输出标准化论文摘要/前言”的单点闭环。  
  3. 开发极简的 Next.js 提交表单对接 Dify API。  
* **交付物**：一次内部组会 Demo 演示。

#### **第二阶段：工作流拆解与平台化化 (第 3 \- 4 周)**

* **目标**：复刻对标产品，将“写论文”拆解为 4-6 个具体的工具卡片供团队使用。  
* **任务**：  
  1. 在前端开发“工具集”面板（如：Step 1 生成大纲、Step 2 扩写前言等）。  
  2. 与团队业务专家（师兄/老师）对接，把每一部分所需的农学专业提示词（Prompt）写死到后台系统中。  
  3. 优化系统 UI，增加加载状态（Loading）和错误反馈机制。  
* **交付物**：系统 V1.0 内部上线，支持多工具协同。

#### **第三阶段：联调抗压与优化 (第 5 \- 6 周)**

* **目标**：根据真实团队反馈，进行调优。  
* **任务**：  
  1. 修复非常规格式输入导致的代码 Bug。  
  2. 优化知识库检索命中率，提升专业词汇的准确性。  
* **交付物**：稳定可用的系统终版及使用手册。

### **六、 部署方案与预算清单 (Budget)**

考虑到高昂的本地算力显卡（GPU）成本，本项目采用\*\*“轻量级服务器计算 \+ 云端大模型推理”\*\*的最高性价比组合。

**首选方案：云端轻量化部署 (强力推荐)**

无需购买昂贵的物理主机，系统随时随地可访问。

* **轻量云服务器租赁** (阿里云/腾讯云，4核16G内存)：约 **1500 \- 2000 元/年**。用于部署网站前端与 Dify 平台。  
* **云端大模型 API 消耗** (DeepSeek/通义千问)：首期测试预充值 **500 \- 1000 元**。按字数计费，足以支撑团队大半年的论文生成需求。  
* **AI 辅助开发耗材** (Cursor 等开发工具)：约 **1500 元/年**。  
* **总预算申请**：**约 4000 \- 4500 元**（仅为购买普通台式机一半的成本，且实现全天候多人并发）。

*(注：如实验室数据涉及极高密级，禁止使用公有云，则备选方案为：以 6000-8000 元预算采购一台高内存的无独立显卡物理主机，放置于实验室内作为局域网服务器即可。)*