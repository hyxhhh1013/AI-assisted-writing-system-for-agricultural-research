"use client";

import { AgentGuidePage } from "../../../academic-paper-studio/components/AgentGuidePage";

/** /academic-paper — Agent 引导页（原八阶段看板已降级，见 MASTER_PLAN） */
export default function AcademicPaperPage() {
  return <AgentGuidePage />;
}
