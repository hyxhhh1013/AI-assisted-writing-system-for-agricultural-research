# 论文智能助手 — 项目演示介绍稿

## 开场：为什么要做这个？

> "我们写一篇 SCI 论文，从初稿到投稿，至少要经历：查文献→读文献→做实验→分析数据→写大纲→写初稿→修改→查重→降重→排版→加参考文献。这个过程动辄 3-6 个月。我做这个工具的目标不是替代你写论文，而是**用 AI 帮你把机械劳动自动化**，让你把时间花在真正需要思考的地方。"

---

## 一、完整功能导览

按写论文的实际流程组织：

### 第一步：建知识库（文献管理）

- 支持**批量上传** PDF，自动按研究方向分类到子目录
- 一键索引：AI 逐篇阅读理解所有文献，建立可检索的文献向量库
- **文献阅读器**：左边看 PDF，右边 AI 对话——可以追问"这篇的核心发现是什么？""用了什么方法？""有哪些局限性？"
- **划词翻译**：选中 PDF 中的英文段落 → AI 用农业/热化学领域专业术语翻译（不是通用翻译），翻译结果支持标注笔记和高亮

### 第二步：项目规划

- 创建论文项目，输入题目 + 研究方向
- AI 基于知识库文献生成结构化大纲
- 大纲可手动调整，保存为项目草稿

### 第三步：数据分析 + 科学作图

**数据分析**：

- 上传实验数据（CSV/Excel）→ AI 自动识别变化趋势
- 用学术语言描述数据规律
- 结合文献库自动对比讨论

**集成科学作图工具（XRD 实验室 + 通用图表）**：

这是这个项目区别于普通 AI 写作工具的一个重要特色——它不只是写文字，还能**生成论文所需的专业图表**。

| 工具 | 功能 | 适用场景 |
|------|------|---------|
| **XRD 峰拟合** | 自动分解重叠峰、扣除背景、标注峰位 | 材料表征、晶体分析 |
| **XRD 晶胞可视化** | 输入晶胞参数 → 3D 晶体结构展示 | 晶体学数据展示 |
| **XRD Bragg 计算** | 根据 d 值/2θ 值计算晶面指数 | 物相鉴定 |
| **XRD 非晶分析** | 非晶相定量、结晶度计算 | 非晶材料分析 |
| **通用图表** | 折线图、柱状图、散点图等 | 实验数据可视化 |
| **流程图生成** | 文字描述 → 自动绘制实验流程图 | 方法学展示 |
| **分子结构图** | SMILES/分子式 → 化学结构式 | 有机化学/天然产物 |
| **机理示意图** | 描述反应路径 → 自动绘制机理图 | 催化/热化学机理 |

以上工具产出的图表均可**一键插入论文正文**对应位置，图片格式为高清 Base64/PNG，可直接用于投稿。

### 第四步：分章节扩写（核心功能）

- **五个章节**独立扩写：摘要、引言、方法、结果与讨论、结论
- 每章有**不同的写作策略**：
  - 引言遵循"背景→问题→现有研究局限→本研究切入点"的逻辑链
  - 结果与讨论按主题分子节，自动编号（如 3.1.1、3.1.2）
  - 结论编号分点列出核心发现
- **自动配图**：AI 在文中标记图表位置，自动调用图表生成器插入
- **段落编辑器**：逐段精细控制，支持 AI 扩写、学术审查、一键修正

### 第五步：质量保障

- **跨章节一致性检查**：自动核对五章之间的术语、数据、逻辑是否矛盾
- **引用重排**：按正文首次出现顺序自动排列参考文献
- **学术核查**：独立 AI 逐条核实每个引用是否真实存在于文献库中
- **查重 + AI 降重**：保持学术原意的前提下降低重复率

### 第六步：导出

- 一键导出 Word (.docx) / Markdown / PDF
- 自动按 SCI / GB/T 7713 / Nature / IEEE 等模板排版
- 参考文献自动格式化

---

## 二、科学依据 — 不是简单"问 ChatGPT"

### 2.1 多 Agent 协作架构（Writer → Verifier → Refiner）

> 类比：自己写初稿 → 导师审稿 → 修改后投稿

| 角色 | 对应的 AI 模型 | 职责 |
|------|---------------|------|
| **Writer（写手）** | DeepSeek | 基于文献库起草初稿 |
| **Verifier（审稿人）** | 智谱 AI | 独立核查每个引用是否真实、数据是否准确 |
| **Refiner（主编）** | 条件触发 | 根据审稿意见逐条修正，被禁止"为通过审查而删除引用" |

**学术依据**：

- [Chain-of-Verification Reduces Hallucination in LLMs (Dhuliawala et al., 2024)](https://arxiv.org/abs/2309.11495) — 用独立模型验证能显著降低幻觉
- [Improving Factuality and Reasoning in Language Models through Multiagent Debate (Du et al., 2023)](https://arxiv.org/abs/2305.14325) — 多模型交叉检验提升事实准确性

### 2.2 RAG 检索增强生成 — 基于真实文献写作

> 普通 ChatGPT：靠训练记忆编内容，引用是虚构的
> 本系统：先检索你的文献库 → 基于真实文献片段写作 → 标注可追溯的引用编号

**混合检索策略**：

1. **BM25 关键词检索**：精确匹配实验术语和关键概念
2. **向量语义检索**：理解查询意图，找到用词不同但意思相近的文献
3. **RRF（倒数秩融合）**：融合两种排序信号，取各自优势
4. **来源去重**：限制每篇文献最多 4 个片段，避免被单篇文献主导

**学术依据**：

- BM25: Robertson & Zaragoza (2009) — 概率检索模型标准
- 语义检索: Sentence-BERT (Reimers & Gurevych, 2019)
- RRF 融合: Cormack et al. (2009) — 多路排序融合理论

### 2.3 引用真实性逐条验证

> ChatGPT 最严重的学术作弊：编造看起来很专业的引用，但论文根本不存在

**验证流程**：

1. Writer 只能引用文献库中 `[参考来源 [n]]` 标记的真实文献
2. Verifier 拿到**被引用文献的完整原文**（不是摘要），逐条比对：
   - ✓ 完全匹配 → 通过
   - ⚠ 核心观点不对应 → 标记「归属错误」
   - ✗ 原文找不到该信息 → 标记「疑似虚构」
3. 用关键词重叠率（NLI 代理指标）辅助筛查

### 2.4 认知负载优化的 Prompt 设计

> 9 条要求塞进一个 prompt → AI 只认真看前 3-4 条

**学术依据**：

- [Lost in the Middle: How Language Models Use Long Contexts (Liu et al., 2023)](https://arxiv.org/abs/2307.03172) — 论文证明了大模型对 prompt **中间部分**的注意力衰减严重

**设计策略**：

- 将 9 条要求精简为 3 组核心原则
- 最重要的放最前（**首因效应**）
- 最关键的禁止事项放最后（**近因效应**）
- 中间只放数据参考和一致性约束

### 2.5 章节感知的检索增强

> 不同章节需要不同类型的文献

| 章节 | 检索侧重点 |
|------|-----------|
| 引言 | 综述、背景、研究现状 |
| 方法 | 实验方法、表征手段 |
| 结果与讨论 | 数据对比、机理分析 |
| 结论 | 展望、应用前景 |

检索时自动注入研究方向关键词（如"生物质热解 碳材料"），在多方向混合文献库中精准定位相关文献。

---

## 三、完整技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户输入（题目 + 章节 + 上下文）         │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  RAG 混合检索（BM25 + 向量语义 → RRF 融合 → 来源去重）      │
│  从 167 篇文献 / 12626 个知识块中找到最相关的 20 个片段      │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Agent 1: Writer（DeepSeek）— 基于文献起草初稿              │
│  Agent 2: Verifier（智谱 AI）— 独立核查引用 + 事实准确性     │
│  Agent 3: Refiner（条件触发）— 逐条修正，禁止删引用逃避审查  │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  后处理：引用重排 + 子节编号 + 自动配图                      │
└────────────────────────┬────────────────────────────────┘
                         ↓
                    最终输出 → 编辑器 → 导出
```

---

## 四、演示流程（12-18 分钟）

1. **打开知识库**（1 min）：展示已索引的文献，打开一篇演示 AI 对话
2. **创建论文项目**（1 min）：输入题目 + 研究方向
3. **生成大纲**（1 min）：展示基于文献库生成的结构化大纲
4. **XRD / 图表工具**（2 min）：快速展示 XRD 峰拟合 + 通用图表的生成和插入
5. **扩写一个章节**（3 min）：用结果与讨论做例子 → AI 流式生成 → 自动配图 → 应用到编辑器
6. **引用重排**（1 min）：一键按正文顺序排列参考文献列表
7. **一致性检查**（1 min）：跨五章检查术语/数据/逻辑矛盾
8. **导出 Word**（1 min）：SCI 格式一键导出

---

## 五、常见问题（Q&A）

**Q: 这和直接问 ChatGPT 有什么区别？**

> ChatGPT 是通用对话，不基于你的文献库，引用是编造的。这个系统每一步都锚定在实验室的真实文献上，有三个 AI 互相审查，引用可追溯到原文。

**Q: AI 写的内容能直接投稿吗？**

> 不能，也不应该。这是**辅助工具**，帮你减少机械劳动——查文献、组织语言、排版引用。核心的科学判断、实验设计、数据解读，必须你自己把关。它加速的是"怎么写"，不是"写什么"。

**Q: 引用真的不会编造吗？**

> 系统有三层防护：Writer 只能引用文献库中存在的真实文献 → Verifier 拿到原文逐条比对 → Refiner 被明确禁止"为通过审查而删除引用"。如果检测到疑似虚构，会标记并提醒你人工核实。

**Q: 支持英文论文吗？**

> 支持。可以切换输出语言（中文/英文），也可以切换排版模板（SCI / Nature / IEEE / GB/T 7713）。

**Q: 多研究方向混合会影响准确性吗？**

> 检索时会注入你的研究方向关键词（如"生物质热解 碳材料"），确保热化学方向的问题优先匹配热化学文献，不会被茶叶加工等其他方向文献干扰。

**Q: 文献越来越多性能会下降吗？**

> 当前 167 篇文献（12626 个知识块）完全无压力。随着文献量增长，后续可以引入向量索引（HNSW）和专用向量数据库来保证检索性能。
